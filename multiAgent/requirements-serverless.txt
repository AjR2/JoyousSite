# Serverless-optimized requirements for Vercel deployment
# Core dependencies
openai==1.3.5
anthropic==0.7.4
asyncpg==0.29.0
python-dotenv==1.0.0
httpx>=0.23.0

# Minimal dependencies for serverless
pydantic==2.5.0
typing-extensions==4.8.0

# Remove heavy dependencies that aren't needed in serverless
# fastapi - not needed for serverless functions
# uvicorn - not needed for serverless functions  
# redis - replaced with database cache
# sqlalchemy - using direct asyncpg for lighter footprint
# alembic - not needed in serverless
# slowapi - not needed in serverless
# pgvector - using simplified embeddings
